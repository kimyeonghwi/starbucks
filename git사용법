터미널에서 하는법

1. git init - 처음 깃 생성 (현재 프로젝트에서 변경사항 추적(버전관리)을 시작한다는 의미 )

- 첫 셋팅 개행 문자 설정 -
2.  git config --global core.autocrlf true 

-사용자 정보 -
-커밋(버전생성)을 위한 정보 등록
3. git config --global user.name 'kimyeonghwi'  이름 추가 

4. git config --global user.email 'pwrkim12@gmail.com' 이메일 추가 

-구성확인-
5. git config --global --list 

---- 여기까지 해주면 일단 깃에 저장해준것이다. ---- 

git add .  이 명령어로 지금 폴더들 전부 추가 해주고 

git log  현재 리스트들 확인 할수 있다. 

git commit -m 'README.MD 여기에 작성해 '  커밋하는 방법 

git status  현재 상태 보기 


//아래 과정을 통해서 origin이라는 이름으로 원격의 저장소를 연결해줌 
git remote add origin 주소  


git push origin master  -> origin에 현재 주소가 연결되어 있고 push를 통해서 원격 저장소에 저장해준다 .

-------------------------------------------------------
브랜치 만들기 

git branch -a (-a 를 붙여서 브랜치를 실행하게 되면 원격 저장소에 있는 브랜치도 확인이 가능하다)

git branch signin(원하는 이름 작성) 새로운 브랜치를 하나 만들어 준다. 

git checkout signin (원하는 브랜치로 이동한다.)