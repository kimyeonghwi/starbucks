터미널에서 하는법

1. git init - 처음 깃 생성 

2.  git config --global core.autocrlf true  첫 셋팅 



git add .  추가 

git log  리스트 출력

git commit -m 'README.MD 여기에 작성해 '  커밋하는 방법 

git status  현재 상태 보기 

git push origin master  -> origin에 현재 주소가 연결되어있다.

